<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
package="com.example.sorojara.tarea">

<application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">
    <activity android:name=".MainActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
    <activity android:name=".Juego" >
        android:parentActivityName="com.example.sorojara.tarea.MainActivity >
    </activity>

    <activity android:name=".HighScores" >
        android:parentActivityName="com.example.sorojara.tarea.MainActivity >
    </activity>
    <activity android:name=".ingreso" />

    <activity android:name=".ingresoHs">
        android:parentActivityName="com.example.sorojara.tarea.MainActivity >
    </activity>
</application>

</manifest>